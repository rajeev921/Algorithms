//1st_prog_Tree creation 


#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<assert.h>
#define NIL -1

typedef struct tree {
 struct tree *l_child;
 int data;
 struct tree *r_child;
}TREE;



void create()
{


}

int main()
{
    TREE *root = (TREE *)malloc(sizeof(TREE));
    if(root ==NULL)
       exit(1);
    else
    {
     root->l_child = NULL;
     root->r_child = NULL;
     root->data    = NIL;
    } 
    while(1)
    {
      printf("\n Enter your choice \n");
      printf("");
      scanf("%d",&ch);

      switch(ch)
      {
         case 1: create(root);
                 break;
         case 2: display();
                 break;
         case 3: exit(2);
         default: printf("\n Wrong choice \n");
      }
}